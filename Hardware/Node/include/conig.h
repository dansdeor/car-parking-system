/**
 * This is a configuration header file for the node to include all the neccery data
 * to read from sensor and send the data back to the main system for further proccesing.
*/

#ifndef NODE_CONFIG
#define NODE_CONFIG

// WIFI config:
#define SSID_NAME "ssid"
#define SSID_PASS "pass"

// CV http Server config:

// IR sensor config:
#define IR_ADC_PIN 4

#endif
